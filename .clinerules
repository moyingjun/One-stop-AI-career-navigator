# 项目架构规范 (Project Architecture Rules)

你现在是这个项目的资深首席架构师。在帮我开发任何功能或修改代码前，必须遵守以下规范：

1. **绝对禁止文件堆叠**：严禁将业务逻辑、数据库操作和路由接口写在同一个文件中。
2. **三层架构强制执行**：
   - /routes: 仅负责处理 HTTP 请求、验证参数、调用 Service。
   - /services: 核心业务逻辑所在。比如：用户注册时发邮件、查重复、加密密码都在这里。
   - /models: 仅定义数据库表结构（Schema）和简单的增删改查。
3. **重构优先**：如果你发现我当前的文件结构不符合上述要求，请在实现新功能前，先提出重构建议。
4. **可读性要求**：所有复杂的逻辑必须有中文 Docstring 注释。代码变量名要语义化，避免使用 a, b, c 这种无意义命名。
5. **增量解释**：每次完成代码修改后，请用三句短话告诉我：你改了哪，为什么要这么改，以及我之后需要注意什么。